/**
 * SurfaceCard was promoted to a `ui/` primitive. This module re-exports it for
 * backward compatibility — prefer importing from `@/ui/surface-card`.
 */
export { SurfaceCard, surfaceCardVariants } from "@/ui/surface-card";
export type { SurfaceCardProps } from "@/ui/surface-card";
